// file dengan variable constant yang akan digunakan di seluruh file

// Buat Aspect Ratio
const ASPECT_RATIO = {
    WIDTH: 16,
    HEIGHT: 9
}

// Buat Ukuran Display
const DISPLAY_PIXEL = {
    WIDTH: 64,
    HEIGHT: 64
}
